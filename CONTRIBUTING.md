# Contributing to Symbiosis

Thank you for contributing! To maintain long-term codebase health, preserve human cognitive control, and ensure predictable AI co-piloting, this repository strictly enforces clean Git hygiene and structured workflows.

---

## 1. Specification-Driven Workflow

Every contribution—whether a bug fix, new feature, architectural refactoring, or codebase analysis—MUST start with a Markdown specification file in the `specs/` directory:

1. Create a spec file following the chronological folder structure: `specs/YYYY/MM/YYYY-MM-DD-short-title.md`.
2. Copy the structure from [`specs/_template.md`](./specs/_template.md).
3. If using an AI coding assistant, submit the spec file to initiate the [`GUARDRAILS.md`](./GUARDRAILS.md) execution algorithm (`[REFINE]`, `[ASK]`, or `[EXECUTE]` modes).

---

## 2. Git Hygiene & Commit Standards

We maintain a linear, clean, and easily auditable Git history. All commits—whether written by humans or generated by AI—must abide by the following rules:

### Atomic Commits
* Each commit must represent a **single, logical unit of work**.
* Avoid grouping unrelated changes (e.g., mixing a bug fix with a formatting cleanup or refactoring) into one commit.

### Self-Contained & Functional
* Every individual commit in the history MUST leave the repository in a working state. 
* Tests must pass, code must compile, and no broken states should be committed, even temporarily.

### Revertibility
* Commits must be designed so that any single commit can be cleanly reverted (`git revert <commit-hash>`) without breaking adjacent, unrelated functionality.

### Scope-Bound Increments
* Keep individual changes small. When collaborating with AI, adhere to the **30–50 lines of code change limit per interaction** before staging and committing.

---

## 3. Commit Message Format

Use clear, imperative commit messages to explain *what* and *why*:

```text
<type>(<scope>): <short summary in imperative mood>

[optional body explaining motivation and architectural rationale]
[optional reference to specification file, e.g., Refs specs/2026/08/2026-08-05-auth.md]
